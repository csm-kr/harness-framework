# 보안

> **Refs**: [API](./API.md) · [ENV](./ENV.md) · [RULES](./RULES.md)

위협 모델, 인증/인가, 비밀 관리, 데이터 보호를 정의한다.

## 위협 모델
| 위협 | 영향 | 대응 |
|---|---|---|
| {예: 미인증 API 접근} | {데이터 유출} | {모든 핸들러에서 세션 검증} |
| {예: IDOR (남의 리소스 접근)} | {권한 상승} | {소유자 검증} |
| {예: XSS} | {세션 탈취} | {출력 이스케이프, CSP} |
| {예: 비밀 노출} | {계정 탈취} | {.env, 서버 전용} |

## 인증
- {방식 (예: NextAuth 세션 쿠키, JWT)}
- {세션 만료·갱신 정책}

## 인가
- {역할/권한 모델 (예: owner / member / public)}
- CRITICAL: 리소스 접근 시 소유자/권한을 서버에서 반드시 검증한다. 엔드포인트별 권한은 [API](./API.md) 참조.

## 비밀 관리
- 비밀은 환경 변수로만 주입한다 ([ENV](./ENV.md)).
- CRITICAL: 비밀 값을 코드·로그·클라이언트 번들에 노출하지 않는다.
- {키 로테이션 정책}

## 데이터 보호
- {전송 구간: HTTPS 강제}
- {저장 구간: 민감 필드 암호화/해시 (예: 비밀번호 bcrypt)}
- {PII 취급·보존 정책}

## 입력 검증
- {모든 API 입력은 스키마 검증 (예: zod)}
- {파일 업로드 타입·크기 제한}
